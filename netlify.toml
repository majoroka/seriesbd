[build]
  command = "vite build"
  functions = "netlify/functions"
  publish = "dist"

[dev]
  command = "vite"
  targetPort = 5173

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200